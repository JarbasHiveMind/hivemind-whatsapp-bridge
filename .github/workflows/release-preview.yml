name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@dev
    with:
      package_name: 'hivemind_whatsapp_bridge'
      version_file: 'hivemind_whatsapp_bridge/version.py'
